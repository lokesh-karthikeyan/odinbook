<%= turbo_stream.update dom_id(Post.new), "" %>
<%= turbo_stream.prepend "posts_feed",
                     partial: "shared/post",
                     locals: {
                       post: @post,
                     } %>
